language: node_js
node_js:
   - "0.12"
   - "0.10"
   - "4"
   - "6"
os:
  - linux
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install python3
  - sudo apt-get install php5-cli
sudo: required
services:
  - docker
